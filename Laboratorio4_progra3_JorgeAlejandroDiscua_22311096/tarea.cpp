#include "tarea.h"

Tarea::Tarea(int d, const QString& m, const QString& t, const QString& desc)
    : dia(d), mes(m), titulo(t), descripcion(desc)
{
    // Inicialización de atributos de la clase base
}

/* Destructor virtual
Tarea::~Tarea() {
    // No se requiere implementar nada específico aquí a menos que tengas recursos dinámicos que liberar
}
*/
